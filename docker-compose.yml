# 범용 Next.js 템플릿용 docker compose.

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: url-shortener:latest
    container_name: url-shortener
    env_file:
      - .env
    ports:
      - '3000:3000'
    restart: unless-stopped
